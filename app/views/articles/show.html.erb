<h1><%= @article.name %></h1>
<h2>Precio: <%= @article.price %></h2>
<h5>Descripción:</h5>
<p><%= @article.description %></p>
<% if user_signed_in? %>
<% unless policy(@article).owner?%>
<%= form_with(model: current_order.order_items.new) do |f| %>
<%= f.hidden_field :order_id, value: current_order.id %>
<%= f.hidden_field :article_id, value: @article.id %>
<%= f.hidden_field :quantity, value: 1 %>
<%= f.submit "Agregar", class: "btn btn-primary" %>
<% end %>
<% end %>
<% else %>
<%= link_to "Registrate para arrendar", new_user_registration_path, class: "btn btn-primary" %>
<% end %>
